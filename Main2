python
import urllib
from selenium import webdriver
from selenium.webdriver.chrome.options import Options
from selenium.webdriver.common.by import By
import time
import re

# ... (previous code remains unchanged)

# Replace 'path_to_webdriver' with the actual path to your webdriver executable
service = Service(executable_path='./chromedriver.exe')

proxy_list = [
    {"ip": "your_proxy_ip", "port": "your_proxy_port", "username": "your_username", "password": "your_password"},
    # Add more proxies in a similar format if needed
]

with open("url.txt", "r", encoding="utf-8") as file:
    urls = file.read().strip().split("\n")

for proxy in proxy_list:
    print(f"Trying proxy: {proxy['ip']}:{proxy['port']}")
    try:
        chrome_options = Options()
        proxy_with_auth = f"{proxy['ip']}:{proxy['port']}@{proxy['username']}:{proxy['password']}"
        chrome_options.add_argument(f"--proxy-server=http://{proxy_with_auth}")

        driver = webdriver.Chrome(service=service, options=chrome_options)

        # Rest of your code for using the proxy
        # ...
        
    except Exception as e:
        print("An error occurred:")
        driver.quit()
        continue
